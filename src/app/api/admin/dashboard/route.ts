import { requireRole } from "@/lib/auth/rbac"; import { prisma } from "@/lib/prisma/client"; import { ok } from "@/lib/http"; export async function GET(){ const s=await requireRole(["ADMIN"]); if("status" in s) return s; const [totalUsers,pendingFreelancers,bookingsToday]=await Promise.all([prisma.user.count(),prisma.freelancerProfile.count({where:{verificationStatus:"submitted"}}),prisma.bookingRequest.count({where:{createdAt:{gte:new Date(new Date().setHours(0,0,0,0))}}})]); return ok({totalUsers,pendingFreelancers,bookingsToday}); }
