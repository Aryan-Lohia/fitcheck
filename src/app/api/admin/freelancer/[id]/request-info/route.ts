import { NextRequest } from "next/server"; import { requireRole } from "@/lib/auth/rbac"; import { prisma } from "@/lib/prisma/client"; import { ok } from "@/lib/http"; export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){ const s=await requireRole(["ADMIN"]); if("status" in s) return s; const body=await req.json(); const {id}=await params; const profile=await prisma.freelancerProfile.update({where:{id},data:{verificationStatus:"needs_more_info",verificationNotes:body.notes||""}}); return ok({profile}); }
